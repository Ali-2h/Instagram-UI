import 'package:flutter/material.dart';

class Addbottomnavicon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("AddScreen",style: TextStyle(color: Colors.black),),
      ),
    );
  }
}