import 'package:flutter/material.dart';

class Favbottomnavicon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("FavScreen",style: TextStyle(color: Colors.black),),
      ),
    );
  }
}