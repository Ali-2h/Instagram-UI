import 'package:flutter/material.dart';

class Searchbottomnavicon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("SearchScreen",style: TextStyle(color: Colors.black),),
      ),
    );
  }
}